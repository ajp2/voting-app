<% include ./header %>

<div class="container">
  <br>
  <h1>Polls</h1>
  <p>Select a poll below to see the results and vote</p>
  <hr>
  <br>
  <% allPolls.forEach(poll => { %>
  <a href="/polls/<%= poll.id %>">
    <%= poll.title %></a>
  <% }) %>
</div>

<% include ./footer %>